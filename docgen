#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
JAR="$SCRIPT_DIR/target/docgen.jar"
if [[ ! -f "$JAR" ]]; then
  echo "docgen: $JAR not found — run 'mvn package' first." >&2
  exit 1
fi
exec java -jar "$JAR" "$@"
