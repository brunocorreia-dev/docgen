#!/usr/bin/env bash
# Run docgen from anywhere: ./docgen <repo-path> [--output <dir>]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec java -jar "$SCRIPT_DIR/target/docgen.jar" "$@"
